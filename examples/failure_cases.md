# Failure Cases

## 1) Model adds explanations
Fix: add "JSON only, no prose" + validation chain.

## 2) Hallucinated values
Fix: "Do not infer unknown values; use null".

## 3) Wrong label due to ambiguity
Fix: include "choose OTHER if ambiguous" + confidence rule.

